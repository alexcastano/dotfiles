#!/bin/bash

if [ -f ~/.vimrc ]; then
  echo "Moving old configuration"
  mv ~/.vimrc ~/.vimrc.bak
fi

echo "Linking configuration"
ln -s ~/.vim/vimrc ~/.vimrc

echo "Download plugins"
cd ~/dotfiles
git submodule update --init

mkdir ~/.vim_backups
mkdir ~/.vim_tmp
