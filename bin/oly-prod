#!/bin/bash


ID=$(aws ec2 describe-instances --filters 'Name=instance.group-name,Values=app-prod-cluster-instance' --query 'Reservations[0].Instances[0].PrivateDnsName')
ID=${ID//\"}
echo $ID

ssh -o ProxyCommand='ssh -W %h:%p "oly-prod-bastion"' $ID "$*"
