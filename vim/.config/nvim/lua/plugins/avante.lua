return {
  {
    "yetone/avante.nvim",
    opts = {
      -- Force the provider to Gemini
      provider = "gemini",

      -- Configure Gemini specifics
      -- gemini = {
      --   model = "gemini-1.5-flash", -- Fast and cheap. Use "gemini-1.5-pro" for complex refactors.
      --   temperature = 0,
      --   max_tokens = 4096,
      -- },
    },
  },
}
